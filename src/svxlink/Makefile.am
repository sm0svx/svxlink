# Import some general items
include $(top_srcdir)/common.mk

# Subdirectories in the order of compilation.
SUBDIRS = trx svxlink modules remotetrx siglevdetcal

# will include everything in these directories, including editor backups
EXTRA_DIST = scripts dtmf_plot makefile.dirs
